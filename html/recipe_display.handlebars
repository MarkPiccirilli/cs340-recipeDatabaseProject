<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>Recipe</title>
        <link rel="stylesheet" href="css/main_stylesheet.css" type="text/css">
    </head>
    <body>
        <h1 class = "title">{{Recipe Name}}</h1>
        <h2>Indredients</h2>
        <ul>
            {{#each ingredient_id}}
            <li>{{ingredient_name}},  {{ingredient_quantity}}</li>
            {{/each}}
        </ul>

        <h2>Cookware</h2>
        <ul>
            {{#each cookware_id}}
            <li>{{cookware_name}}</li>
            {{/each}}
        </ul>
        <h2>Instructions</h2>
        <p>{{instruction}}</p>
        <h2>Contributor: {{contributor}}</h2>
        <h4>Date Contriuted: {{date_contributed}}</h4>
        <a href="contributor.handlebars" class="link ">link to contributor page</a>
        <h4>Meal Type: {{meal_type}}</h4>
        <h4>Ethnic Cuisine: {{ethnic_cuisine}}</h4>

        <a href="index.html" class="link ">Back to home page</a>
    </body>
</html>
